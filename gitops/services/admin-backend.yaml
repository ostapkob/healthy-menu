# Admin Backend Service
name: admin-backend
tag: "137"

# Service port
port: 8000
targetPort: 8000

# Vault configuration
vaultEnabled: "true"
vaultRole: "backend-role"

# Istio configuration
istioEnabled: "true"
istioPeerAuthMode: "PERMISSIVE"
istioAllowInternal: "true"
