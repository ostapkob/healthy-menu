# Order Frontend Service
name: order-frontend
tag: "153"

# Service port
port: 80
targetPort: 80

# Vault configuration (frontend не требует доступа к секретам)
vaultEnabled: "false"
vaultRole: ""

# Istio configuration
istioEnabled: "true"
istioPeerAuthMode: "PERMISSIVE"
istioAllowInternal: "false"
