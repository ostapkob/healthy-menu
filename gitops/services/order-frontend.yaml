# Order Frontend Service
name: order-frontend
tag: "153"

# Service port
port: 80
targetPort: 80

# Istio configuration
istio:
  enabled: true
  peerAuthentication:
    mode: "PERMISSIVE"
  authorizationPolicy:
    allowInternal: false
