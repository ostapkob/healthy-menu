# Admin Frontend Service
name: admin-frontend
tag: "139"

# Service port
port: 80
targetPort: 80

# Istio configuration
istio:
  enabled: true
  peerAuthentication:
    mode: "PERMISSIVE"
  authorizationPolicy:
    allowInternal: false  # Frontend принимает только от Gateway
