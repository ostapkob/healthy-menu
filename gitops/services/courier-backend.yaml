# Courier Backend Service
name: courier-backend
tag: "158"

# Service port
port: 8000
targetPort: 8000

# Vault configuration
vaultEnabled: "true"
vaultRole: "backend-role"

# Istio configuration
istioEnabled: "true"
istioPeerAuthMode: "PERMISSIVE"
istioAllowInternal: "true"
